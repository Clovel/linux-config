[user]
    email = cd.clovel19@gmail.com
    name = Clovis Durand
[http]
    sslVerify = false
[alias]
    co = checkout
    st = status -s
    amend = commit --amend
    skip = rebase --skip
    continue = rebase --continue
    sb = submodule
    la = log --graph --color --abbrev-commit --decorate --format=format:'%C(bold blue)%h%C(reset) - %C(bold green)(%ar)%C(reset) %C(white)%s%C(reset) %C(dim white)- %an%C(reset)%C(bold yellow)%d%C(reset)' --all
[core]
    editor = vim
[credential]
    helper = cache
[sendemail]
    smtpserver = smtp.gmail.com
    smtpuser = cd.clovel19@gmail.com
    smtpencryption = tls
    smtpserverport = 587
    supresscc = self

